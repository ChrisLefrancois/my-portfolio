<%= simple_form_for contact do |f| %>
  <%= f.input :name, required: true %>
  <%= f.input :email, as: :email, required: true %>
  <%= f.input :message, as: :text, required: true, placeholder: 'Write your email...' %>
  <%= f.submit "Send", class: "btn button-submit" %>
<% end %>
